@model IEnumerable<Koolitus.Models.koolitused>

@{
    ViewBag.Title = "Koolitused";
}

<h2>Koolitused</h2>

<p>
    @Html.ActionLink("Loo uus", "Koolitusedс")
</p>
<table class="table">
    <tr>
        <th>
            Koolitusnimetus
        </th>
        <th>
            Koolituskirjeldus
        </th>
        <th>
            Opetajaid
        </th>
        <th>
            koolitusehind
        </th>
        <th>
            koolitusemaht
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Koolitusnimetus)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Koolituskirjeldus)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Opetajaid)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.koolitusehind)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.koolitusemaht)
        </td>
        <td>
            @Html.ActionLink("Muuda", "Edit", new { id=item.id }) |
            
            @Html.ActionLink("Kustuta", "KoolitusedD", new { id=item.id })
        </td>
    </tr>
}

</table>
